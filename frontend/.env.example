# Syncfusion License Key
VITE_SYNCFUSION_LICENSE_KEY=Ngo9BigBOggjGyl/Vkd+XU9FcVRDX3xKf0x/TGpQb19xflBPallYVBYiSV9jS3tSdEVnWX1beXFQQmZYU091Xg==

# API Configuration
VITE_API_BASE_URL=http://localhost:5000/api

# E2E (Playwright)
# Frontend URL — must match vite.config.ts server.port (default 5000)
# PLAYWRIGHT_BASE_URL=http://localhost:5000
# Backend API URL — only needed for authenticated tests (requires .NET backend)
# PLAYWRIGHT_API_BASE_URL=http://localhost:5001
# Test credentials — only needed for authenticated E2E (requires .NET backend + DB)
# TEST_EMAIL=                    # or E2E_TEST_USER_EMAIL
# TEST_PASSWORD=                 # or E2E_TEST_USER_PASSWORD
