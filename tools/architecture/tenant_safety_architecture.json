{
  "description": "Source of truth for the tenant-safety architecture diagram in backend/docs/architecture/SECURITY_AND_TENANT_SAFETY_ARCHITECTURE.md. Edit this file and run tools/architecture/generate_tenant_safety_diagram.ps1 to regenerate the diagram.",
  "external_inputs": [
    { "id": "API", "label": "API requests" },
    { "id": "JOBS", "label": "Background jobs" },
    { "id": "WEB", "label": "Webhook ingestion" },
    { "id": "EVT", "label": "Event replay / dispatch" },
    { "id": "ADM", "label": "Admin / platform operations" }
  ],
  "identity_resolution": {
    "subgraph_label": "🔐 Identity & tenant resolution",
    "nodes": [
      { "id": "TGM", "label": "TenantGuardMiddleware" },
      { "id": "RESOLVE", "label": "Tenant resolution from request / job / event / webhook" },
      { "id": "PROP", "label": "Company ownership propagation" }
    ],
    "edges": ["TGM --> RESOLVE", "RESOLVE --> PROP"]
  },
  "execution": {
    "subgraph_label": "⚙️ Execution safety (standard boundary)",
    "boundary": { "id": "TSE", "label": "TenantScopeExecutor" },
    "modes": [
      { "id": "TSE_T", "label": "Tenant-scoped execution" },
      { "id": "TSE_P", "label": "Platform bypass (exceptional only)" },
      { "id": "TSE_N", "label": "Nullable-company path (explicitly justified only)" }
    ]
  },
  "guards": {
    "subgraph_label": "🛡️ Application guard layer",
    "items": [
      { "id": "SI", "label": "SiWorkflowGuard" },
      { "id": "FIN", "label": "FinancialIsolationGuard" },
      { "id": "ESC", "label": "EventStoreConsistencyGuard" }
    ]
  },
  "persistence": {
    "subgraph_label": "🔒 Persistence safety",
    "nodes": [
      { "id": "EF", "label": "EF global query filters" },
      { "id": "TSG", "label": "TenantSafetyGuard on SaveChanges" },
      { "id": "TSG_NOTE", "label": "(final fail-closed guard)" }
    ],
    "edges": ["EF --> TSG", "TSG --> TSG_NOTE"]
  },
  "data_layer": {
    "subgraph_label": "📦 Data / platform layer",
    "nodes": [
      { "id": "PG", "label": "PostgreSQL / persistence boundary" },
      { "id": "STORE", "label": "Event store / operational records" },
      { "id": "NOTIF", "label": "Notifications / jobs" }
    ]
  },
  "observability": {
    "subgraph_label": "📊 Observability & verification",
    "nodes": [
      { "id": "OPS", "label": "Operations overview endpoint" },
      { "id": "AUDIT", "label": "Audit logs" },
      { "id": "REPORTS", "label": "Validation / remediation reports" },
      { "id": "SUITE", "label": "Tenant-safety regression suite" }
    ]
  },
  "flow_edges": [
    "EXT --> RES",
    "RES --> EXEC",
    "EXEC --> GUARD",
    "GUARD --> PERS",
    "PERS --> DATA",
    "DATA -.-> OBS",
    "PERS -.-> OBS"
  ],
  "intentional_exceptions": [
    {
      "name": "DatabaseSeeder",
      "purpose": "One-time bootstrap/setup seeding. Enter before seeding, Exit in finally. Process-bound."
    },
    {
      "name": "ApplicationDbContextFactory.CreateDbContext",
      "purpose": "Design-time EF Core factory (e.g. migrations, tooling). Enter in CreateDbContext; no Exit (process exits)."
    }
  ],
  "exceptions_footer": "All other runtime operational paths use TenantScopeExecutor; no manual scope or bypass management in hosted services, schedulers, dispatchers, replay, webhooks, or workers."
}
