apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cephasops-api-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: cephasops.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cephasops-api
                port:
                  number: 80
